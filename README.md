# anagram

## Prerequisites
1. python 3.x

## Usage
1. Checkout the file
2. Make sure that the file has permission to run. If not, then run the following command to make it executable: `chmod a+x anagram.py`
3. Run the following command to execute the program: `./anagram.py <word to generate anagrams for>`
